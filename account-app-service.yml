  apiVersion: v1
  kind: Service
  metadata:
    name: account
    labels:
      name: account-service
      app: pod-healthcheck-demo-app
  spec:
    type: LoadBalancer
    ports:
       port: 80
       targetPort: 80
    selector:
      name: account-app-pod
      app: account-demo-app
