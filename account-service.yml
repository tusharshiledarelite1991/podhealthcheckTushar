apiVersion: v1
kind: Service
metadata:
  name: account-service
  labels:
    name: account-service
    app: pod-healthcheck-demo-app
spec:
  selector:
    app: account-app-pod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

